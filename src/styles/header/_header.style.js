import {StyleSheet} from 'react-native';

export const _headerMerkar = StyleSheet.create({
  icon: {
    position: 'relative',
    fontSize: 235,
    color: 'black',
    left: '55%',
    overflow: 'hidden',
  },
});
